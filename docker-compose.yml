services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: elegance_journal
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - elegance_postgres:/var/lib/postgresql/data

volumes:
  elegance_postgres:
